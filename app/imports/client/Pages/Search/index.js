import React, {
  PropTypes,
} from 'react'
import {
  compose,
  pure,
} from 'recompose'
import container from './container'

function Search(){
  return (
  )
}

Search.propTypes = {
}

export default compose(
  container,
  pure,
)(Search)
